# 基于多模态大模型的校园返校时段车辆违停感知与优化研究 Web UI

## 项目简介

这是一个基于 **Vue 3 + Flask** 的前后端分离 AI 智能助手应用，专为校园车辆违停感知与优化研究设计。系统集成了阿里云 DashScope API（Qwen 模型），配合 Gemini 和 QVQ-Max 的多模态分析能力，能够通过对话交互生成详尽的违停分析报告、原因诊断及处罚建议。

项目已全新改版，采用现代化的 **AI Chatbot** 对话界面，提供极简的纯白视觉体验和流畅的流式响应交互。

## 🌟 核心特性

- **🤖 AI 智能对话**：采用类似于 ChatGPT 的对话界面，支持上下文多轮交互。
- **⚡️ SSE 流式响应**：实时打字机效果显示 AI 生成内容，无需等待完全生成。
- **🎨 现代化 UI 设计**：
  - **纯净视觉**：全站采用纯白背景与 `#5b6eae` 科技蓝主题色。
  - **顶部导航**：集成外部大模型链接（Gemini/Qwen）、新建对话及个人中心。
  - **响应式布局**：完美适配桌面端、平板及移动端设备。
- **📝 Markdown 渲染**：支持代码高亮、表格、列表等富文本格式输出。
- **👤 用户系统**：
  - 完整的注册/登录流程。
  - 个人中心支持修改用户名、邮箱及密码。
- **🚀 一键启动**：提供自动化脚本，零配置快速启动前后端环境。

## 🛠 技术栈

### 前端 (Frontend)
- **Vue 3** - 渐进式 JavaScript 框架 (Composition API)
- **TypeScript** - 强类型语言支持
- **Vite** - 下一代前端构建工具
- **Element Plus** - 企业级 UI 组件库
- **Pinia** - 状态管理库
- **Markdown-it** - Markdown 解析与渲染

### 后端 (Backend)
- **Flask** - 轻量级 Python Web 框架
- **DashScope** - 阿里云模型服务 SDK
- **Flask-CORS** - 跨域资源共享处理

## 🚀 快速开始

### 方法一：一键启动（推荐）

项目包含自动化启动脚本，自动检测环境、安装依赖并运行服务。

#### MacOS / Linux
```bash
./scripts/start.sh
```

#### Windows
```bash
scripts\start.bat
```

**启动脚本会自动执行以下步骤：**
1. 检查 Node.js 和 Python 环境
2. 安装 Python 依赖 (`requirements.txt`)
3. 启动 Flask 后端 (端口 5001)
4. 安装 Node.js 依赖 (`package.json`)
5. 启动 Vite 前端开发服务器 (端口 3000)

启动成功后，访问 **http://localhost:3000** 即可使用。

### 方法二：手动启动

#### 1. 后端配置
```bash
cd backend
# 创建环境变量文件，填入你的 DashScope API Key
echo "DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx" > .env

# 安装依赖并启动
pip install -r requirements.txt
python app.py
```

#### 2. 前端配置
```bash
cd frontend
# 安装依赖并启动
npm install
npm run dev
```

## 📖 使用指南

1. **准备工作**：
   - 使用 [Gemini](https://gemini.google.com) 分析违停视频/图片获取初步报告。
   - 使用 QVQ-Max 精准识别违停车牌号。

2. **开始分析**：
   - 登录系统后，点击首页的 **"分析违停报告并替换车牌号"** 快捷指令。
   - 或直接在输入框粘贴 Gemini 分析报告及 QVQ-Max 识别的车牌。
   - AI 将自动整合信息，生成符合《道路交通安全法》的处罚建议。

3. **个人管理**：
   - 点击右上角用户名，可随时修改个人信息或退出登录。

## 📂 项目结构

```
WebUI/
├── backend/              # Flask 后端代码
│   ├── app.py           # 应用入口
│   └── requirements.txt # Python 依赖列表
│
├── frontend/            # Vue 3 前端代码
│   ├── src/
│   │   ├── api/        # API 接口封装
│   │   ├── components/ # 公共组件 (ChatInput, ChatMessage, AppHeader等)
│   │   ├── stores/     # Pinia 状态 (Auth, Chat)
│   │   ├── views/      # 页面视图 (ChatPage, LoginPage, RegisterPage)
│   │   └── style.css   # 全局样式配置
│   └── vite.config.ts  # Vite 配置
│
├── scripts/             # 启动脚本
│   ├── start.sh        # Shell 脚本
│   └── start.bat       # Batch 脚本
│
└── README.md           # 项目文档
```

## ❓ 常见问题

**Q: 启动脚本提示 "permission denied"?**
A: 请运行 `chmod +x scripts/start.sh` 赋予执行权限。

**Q: 注册时提示网络错误?**
A: 请确保后端服务 (Port 5001) 已正常启动，并且前端代理配置正确 (`vite.config.ts`)。

**Q: 如何获取 API Key?**
A: 前往 [阿里云百炼控制台](https://dashscope.console.aliyun.com/) 开通 DashScope 服务并创建 API Key。

## 📄 许可证

本项目仅供学术研究与课程设计使用。
